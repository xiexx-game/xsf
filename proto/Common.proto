// common global define

syntax = "proto3";

package xsf_pb;


// 角色属性
enum ActorProp {
    Nickname = 0;             // 昵称
    ActorID = 1;              // actor_id
    Headicon = 2;             // 头像
    CreateTime = 3;           // 出生时间

    Max = 4;
}